# Toolchain checkouts and generated audio — never committed.
# Authoritative fixtures live in fixtures/ (tracked) and are NOT ignored.
.build/
# Audio artifacts each run produces for the operator to inspect/decode.
out/
